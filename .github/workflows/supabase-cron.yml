name: Supabase Log Listeners Cron


on:
  schedule:
    - cron: '*/1 * * * *' # cada minuto
  workflow_dispatch:

jobs:
  call-supabase-function:
    runs-on: ubuntu-latest
    steps:
      - name: Call Supabase Edge Function
        run: |
          curl -X POST https://ukygfdjxhmlqhxohnvzi.supabase.co/functions/v1/log_listeners
